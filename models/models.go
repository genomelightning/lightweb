package models

import (
// "log"
// "time"

// "github.com/genomelightning/lightning/cytomap"
)

var (
// HgCytoMaps = make(map[int]*cytomap.CytoMap)
)

func init() {
	// log.Println("Start loading genome data...")
	// start := time.Now()

	// // Load genome data.
	// cm, err := cytomap.ParseCytoMap(19, "data/ucsc.cytomap.hg19.txt")
	// if err != nil {
	// 	log.Fatalf("cytomap.ParseCytoMap: %v", err)
	// }
	// log.Printf("Total rules: %d(hg%d)", len(cm.Rules), cm.Hg)
	// HgCytoMaps[cm.Hg] = cm

	// // n, err := cm.PasreTiles()
	// // if err != nil {
	// // 	log.Fatalf("cytomap.PasreTiles: %v", err)
	// // }
	// // log.Printf("Total tiles: %d(hg%d)", n, cm.Hg)
	// log.Printf("Load genome spent: %v", time.Since(start))
}
